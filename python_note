# list 바꿀 수 있음
days = ["M", "T", "W"]

# tuple 바꿀 수 없음
days = ("M", "T", "W") -> tuple

# dictionary = {}
jungwoo = {
    "name": "jungwoo",
    "age": 25,
    "korean": True,
    "fav_food": ["kimchi, "sashimi"]
}
추가 삭제 등도 가능
jungwoo["handsome"] = True

함수의 argument에 디폴드 value를 정해줘서
인자값을 받지 못했을 때의 에러를 방지하고 값을 정해줄 수 있음

from 총모듈 import 쓸모듈
    사용할 모듈만 import가능(효율적)
from math import ceil as hi
    함수의 이름을 hi라는 이름으로 사용가능

# 문자열 검색
    find() 찾고자 하는 문자열이 존재하는 경우, 시작 인덱스값을 리턴
    in, not in 맞으면 True

# 콘솔 초기화
    import os
    os.system('cls')

# requests
    requests.get('url')
    url 받아오기. 입력한 url이 온라인 상태가 아니면 없으면 에러남

# 배열
    array[-1]을하면 뒤에서 첫번째 값을 나타냄
    array[:-1]은 뒤에서 첫번째 값을 제외한 것들을 나타냄