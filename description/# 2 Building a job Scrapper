--------------------------------------------------------------------------------------------------
# 2.0 What is Web Scrapping
    (1) 웹 상의 데이터를 추출하는 것
--------------------------------------------------------------------------------------------------
# 2.1 What are We Building
    (1) 일자리 사이트에서 정보 가져오기
        https://indeed.com/jobs
--------------------------------------------------------------------------------------------------
# 2.2 Navigating with Python
    (1) url에 접근
        https://www.indeed.com/jobs?q=python&limit=50
    (2) 페이지가 몇 개인지 파악
    (3) 페이지 하나씩 들어가줘야 한다
    (4) requests 를 사용한다 (파이썬 만으로도  url의 정보를 가져올 수는 있다)
            파이썬에서 요청을 만드는 기능을 모아 놓은 패키지 (라이브러리)
            requests.get("url")
    (5) url에서 받아온 html 정보를 가져오기위해 beautifulSoup4 을 사용한다
        BeautifulSoup는 html에서 정보를 추출하기에 유용한 패키지다
--------------------------------------------------------------------------------------------------
# 2.3 Extracting indeed Pages
    (1) from bs4 import BeautifulSoup 으로 import 한다
    (2) indeed_soup = BeautifulSoup(indeed_result.text, 'html.parser')
        html 을 가져온다
    (3) soup.find("Element", {"class":"className"})
        해당하는 Element를 하나 가져온다
    (4) find_all() 해당하는 Element를 List 형식으로 가져온다
    (5) [-1]은 마지막에서부터 시작해서 첫 item을 나타낸다
--------------------------------------------------------------------------------------------------
# 2.4 Extracting indeed Pages part Two
    (1) 
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------
--------------------------------------------------------------------------------------------------